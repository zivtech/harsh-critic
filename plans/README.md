# Implementation Plans

Generated by the improve skill on 2026-06-12. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

These plans were drafted after a standard repo audit and then reviewed with the harsh-critic protocol before finalization. The harsh-critic pass forced three revisions: make the verification baseline extensible instead of purely documentary, state the `data-critic` canonical-source rule explicitly, and order the two docs plans to avoid conflicts in `docs/index.html`.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a prompt-surface verification baseline | P1 | M | - | DONE |
| 002 | Canonicalize duplicate data-critic surfaces | P1 | M | 001 | DONE |
| 003 | Downgrade public benchmark claims to historical evidence | P1 | S | 001 | DONE |
| 004 | Fix label-fragile false-positive chart badges | P2 | S | 001, 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 depends on 001 because duplicate-surface drift should become a verifier check after the initial verifier exists.
- 003 depends on 001 because benchmark-provenance caveats should become a verifier check, not only a prose edit.
- 004 depends on 003 because both touch `docs/index.html`; run the benchmark-caveat update first, then adjust the chart logic against the current file.

## Findings considered and rejected

- None. The user selected all audited findings for planning.

## Harsh-critic review notes

- Initial draft weakness: the verification plan risked becoming another dead checklist. Revision: plan 001 creates a passing baseline and named extension points; later plans add concrete failing-then-fixed checks.
- Initial draft weakness: the data duplicate fix could have copied text blindly and made path-specific notes inaccurate. Revision: plan 002 makes `zivtech-data-skills/critic` the canonical behavioral source and compares the load-bearing protocol sections, not every surrounding sentence.
- Initial draft weakness: the docs plans both touched `docs/index.html` without dependency order. Revision: plan 004 now depends on plan 003 and uses drift checks around `drawFPRChart`.
